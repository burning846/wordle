# Neon Postgres connection string, from the Storage tab of the Vercel project.
# Used by the API routes at runtime and by `npm run migrate`.
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
